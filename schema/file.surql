DEFINE DATABASE IF NOT EXISTS demo;

USE DB demo;

DEFINE TABLE IF NOT EXISTS files SCHEMAFULL;

DEFINE FIELD IF NOT EXISTS key ON TABLE files TYPE string;
DEFINE FIELD IF NOT EXISTS name ON TABLE files TYPE string;
DEFINE FIELD IF NOT EXISTS url ON TABLE files TYPE string;  
